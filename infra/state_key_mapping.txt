# infra/state_key_mapping.txt

# <module_path>:<state_key>

remote-state-backend:zaki-terraform-remote-state/terraform.tfstate

# Usage
# 1. Create remote state backend
# ./run.sh --account <ACCOUNT_ID> --region <REGION> --prefix <BUCKET_PREFIX>
# 2. Run terraform init
# terraform init -backend-config="bucket=${REMOTE_STATE_BUCKET}" -backend-config="key=${REMOTE_STATE_KEY}" -backend-config="region=${REMOTE_STATE_REGION}"